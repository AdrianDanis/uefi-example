[package]
name = "uefi-example"
version = "0.1.0"
authors = ["Adrian Danis <rambobones@gmail.com>"]
license = "MIT"

[dependencies]
uefi = { git = "https://github.com/AdrianDanis/uefi-rs" }
rlibc = "1.0.0"
fencedvar = { path = "fencedvar" }

[profile.release]
lto = true
